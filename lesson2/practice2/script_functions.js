//1.Напишите функцию max(a,b), которая возвращает наибольшее из чисел a и b.


/*function max(a, b) {
    if (a > b) {
        return a;
    } else {
        return b;
    }
    return a > b ? a : b
}

max(10, 7)*/






//2.Написать функцию, которая вычисляет среднее арифметическое элементов , переданных ей в качестве аргументов. Пусть будет передано 3 аргумента.

/*function average(parm1, parm2, parm3) {
    return (parm1 + parm2 + parm3) / 3;
}

average(2, 4, 6)*/


//const average = (params1, params2, params3) => (params1 + params2 + params3) / 3





//3.Напишите функцию pow(x,n), которая возвращает x в степени n. Иначе говоря, умножает x на себя n раз и возвращает результат.


/*const pow = (x,n) => x**n*/

/*function pow(x, n) {
    // let result = Math.pow(x, n)
    // return result
    
    return x**n


    /*let result = 1
    for (let i = 0; i < n; i++) {
        result *= x
    }
    return result
}

console.log(pow(2, 3));
console.log(pow(10, 3));*/
